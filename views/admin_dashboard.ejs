<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/vendor/sweetalert2/sweetalert2.min.css">
    <link rel="stylesheet" href="/css/custom.css?v=1.3">
    <link rel="stylesheet" href="/animations.css">
</head>

<body class="admin-page">
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-lg animated fadeIn">
                    <div class="card-header bg-primary text-white animated slideInRight">
                        <h4 class="mb-0 animated bounceIn"><i class="fas fa-user-shield me-2"></i>Admin Dashboard</h4>
                    </div>
                    <div class="card-body">
                        <div class="text-center mb-4">
                            <i class="fas fa-search fa-3x text-primary mb-2 animated bounceIn"></i>
                            <h5 class="mb-3 animated slideInLeft">View Student Results</h5>
                            <p class="text-muted animated slideInRight">Enter the roll number to view results</p>
                        </div>
                        <form action="/admin/results" method="GET">
                            <div class="mb-4">
                                <label for="roll_number" class="form-label">Roll Number</label>
                                <input type="text" class="full-width-input" id="roll_number" name="roll_number" required
                                    pattern="\d{3}" placeholder="Enter roll number">
                            </div>
                            <div class="text-center">
                                <button type="submit" class="btn btn-primary btn-lg animated pulse">View Results</button>
                            </div>
                        </form>
                        <div class="text-center mt-4 button-container">
                            <a href="/admin/logout" class="btn btn-success btn-lg animated glow text-decoration-none">
                                <i class="fas fa-sign-out-alt me-1"></i>Logout
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/vendor/sweetalert2/sweetalert2.all.min.js"></script>
</body>

</html>
