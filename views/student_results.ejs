<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Results</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/vendor/sweetalert2/sweetalert2.min.css">
    <link rel="stylesheet" href="/css/custom.css?v=1.3">
    <link rel="stylesheet" href="/animations.css">
</head>
<body class="dashboard-page">
    <div class="container-fluid my-5">
        <div class="text-center mb-5">
            <h1 class="animated bounceIn"><i class="fas fa-poll-h me-2"></i>Results for Roll Number: <%= roll_number %></h1>
        </div>
        <% if (results.length > 0) { %>
        <% results.forEach(result => { %>
        <div class="card mb-4 animated fadeInUp shadow-lg" style="width: 100%; margin: auto;">
            <div class="card-header bg-light text-center">
                <h2 class="mb-0">Student Report Card</h2>
            </div>
            <div class="card-body pb-4" >
                <div class="row">
                    <div class="col-md-6" style="text-align: center;">
                        <p><strong>Name:</strong> <%= result.name %></p>
                        <p><strong>Aptitude Score:</strong> <%= result.aptitude_score %></p>
                        <p><strong>Technical Score:</strong> <%= result.technical_score %></p>
                        <p><strong>Verbal Score:</strong> <%= result.verbal_score %></p>
                        <p><strong>Programming Score:</strong> <%= result.programming_score %></p>
                    </div>
                    <div class="col-md-6" style="text-align: center;">
                        <p><strong>General Awareness Score:</strong> <%= result.general_awareness_score %></p>
                        <p><strong>Tier:</strong> <span class="badge bg-info"><%= result.tier %></span></p>
                        <p><strong>Auto Submitted:</strong> <%= result.is_auto_submitted ? 'Yes' : 'No' %></p>
                    </div>
                </div>
                <div class="mt-3">
                    <p style="text-align:center;"><strong>Recommendations:</strong></p>
                    <p class="text-muted" style="word-wrap: break-word; text-align: center;"><%= result.recommendations %></p>
                </div>
                <div class="text-center mt-3">
                    <a href="/download-pdf?result_id=<%= result.id %>" class="btn btn-success text-decoration-none"><i class="fas fa-download me-2"></i>Download PDF</a>
                </div>

            </div>
        </div>
        <% }); %>
        <% } else { %>
        <div class="alert alert-warning text-center animated tada no-results-alert" role="alert">
            <i class="fas fa-exclamation-triangle fa-sm me-2"></i> No results found for this number
        </div>
        <% } %>
        <div class="text-center mt-4 animated fadeInUp button-container">
            <a href="/" class="btn btn-primary animated pulse text-decoration-none"><i class="fas fa-arrow-left me-2"></i>Back to Login</a>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>